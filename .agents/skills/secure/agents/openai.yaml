interface:
  display_name: "Secure"
  short_description: "Review changes for security risks"
  default_prompt: "Use $secure to review this change for auth issues, input validation gaps, secret handling mistakes, abuse paths, risky dependencies, and other security concerns before release."
